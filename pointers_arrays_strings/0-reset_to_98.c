#include "main.h"
/**
* fonction qui prend un pointeur vers un int en parametre et 
* met à jour la valeur à laquelle il pointe pour qu'elle soit egal.
*/
int main(){
int num = 42;
reset_to_98(&num);
return (0);
