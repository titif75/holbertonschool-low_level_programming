#include "main.h"  /* Inclure l'en-tête contenant les déclarations des fonctions */
int _islower(int c);  /* Déclare la fonction _islower */
void print_alphabet_x10(void);  /* Déclare la fonction print_alphabet_x10 */
int main(void)
{
print_alphabet_x10();  /* Appelle la fonction print_alphabet_x10 */
return (0);  /* Retourne 0 pour indiquer que tout s'est bien passé */
}
