#include <stdio.h>
#include "main.h"
/**
* main - écrire une fonction qui détecte la lettre  en majuscule ou minuscule
*/
Return : 0
int main(void){
int _isalpha(int c){
if(( c>="A" && c<="Z") || ( c>="a" && c<="z" )){
return 1;
}
return 0;
} 

